# Project Skill Instructions

When the user types `/design-audit` or says `Run DESIGN_IMPLEMENTATION_AUDIT`, use the Claude Code skill located at:

```
.claude/skills/design-audit/SKILL.md
```

Always return the report using the skill output format defined in that file.
